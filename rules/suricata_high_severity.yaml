id: "suricata_high_severity"
description: "Suricata alert with high severity (1 or 2)"
source: "suricata"

# If Suricata alert.severity <= this, we trigger an incident
min_severity: 2

default_severity: "high"
technique: "T1071.001"  # e.g. Application Layer Protocol
